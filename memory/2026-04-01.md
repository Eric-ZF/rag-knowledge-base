# 2026-04-01 Daily Notes

## RAG 学术知识库项目 — Phase 0.6/1 重大更新

### 技术修复

**Phase 0.6 持久化（commit 9ad577d）**
- `data.py`：统一持久化层，JSON 原子写入
- `papers_db.json`：论文数据每次写入自动落盘，重启不丢失
- `users_db.json`：用户数据 + papers 列表持久化
- `start_backend.py`：简化，无需手动注入，数据启动时自动从 JSON 恢复

**Phase 1 Embedding 冲突修复（commit ea7d789）**
- 根因：`query_texts` 让 ChromaDB 内置 embedder 产生 384d，但 ChromaDB 存储的是 BGE 1024d，维度不匹配
- 修复：`hybrid_search` 改用 `query_embeddings` 传入预计算 BGE 1024d 向量，绕过 ChromaDB 内置编码器
- 状态：新 PDF 上传自动用 BGE 1024d 索引；旧 22 chunks 继续可用

**前端 Bug Fixes**
- `api()` 函数 `r.json()` 被调用两次 → 错误信息被吞，登录"无反应"（commit a96d367）
- `escapeHtml` 函数声明被误删 → JS 语法错误 "Illegal return statement"（commit 0d0a325）

**SSE 实时进度（commit a1d466a）**
- `GET /papers/{paper_id}/events` SSE 端点
- 前端 `uploadFile()` 上传后连接 SSE，实时显示进度条
- 阶段：parsing → chunking → embedding → indexing → complete

### 当前系统状态
- Backend: http://124.156.204.163:8000 ✅ 运行中
- Frontend: http://124.156.204.163:8080 ✅ 运行中
- ChromaDB: /root/.openclaw/rag-data/chromadb
- 现有 collection: user_1d2a4dc3_550f_4f89_b97b_2b057705381c（22 chunks, EU CBAM 论文）
- Embedding: BAAI/bge-large-zh-v1.5（1024d）
- LLM: MiniMax M2 via MiniMax API

### 账户信息
- 邮箱：bosstest@boss.io
- 密码：BossPhase0（bcrypt hash 已升级）
- Plan：pro

### gstack Skills 已安装（21个）
位于 ~/.openclaw/workspace/skills/，包括 gstack-ceo, gstack-plan-eng, gstack-investigate, gstack-review, gstack-ship, gstack-canary 等

### CEO 产品方向建议（今日）
- 当前 Phase 0 验证的是"技术可行性" ✅
- **市场需求尚未验证** — 只有 boss 自己一个用户
- 建议：找 5-10 个真实研究员/学生做用户访谈，验证付费意愿
- 产品方向建议：从"RAG 搜索引擎"转向"AI 研究助手"（跨论文知识图谱、自动发现矛盾结论）
- 定价：¥49/月（按领域订阅）或企业版 ¥999/月（未经验证）
